# Only skills/ and agents/ are tracked — exclude everything else

# Sensitive credentials
.credentials.json

# Runtime/ephemeral
sessions/
session-env/
mcp-needs-auth-cache.json
.last-cleanup
.last-update-result.json
history.jsonl

# Local history & snapshots
file-history/
shell-snapshots/
backups/
cache/
.gitignore

# Config & IDE
settings.json
ide/

# etc
scripts/
plans/
ecc/
plugins/
agents/
commands/
hooks/
skills/grill-with-docs

# Per-project memory
projects/
