## To-Do List

    - stock-analyse
        - set default for sort as newest date
        - if have same symbol_analyse group it to one => so UI still same
            - when click on symbol thaat have more than 1 => expand to show all (and can collapse if click again)

    - resume/cv
        - html format w/ option to download .pdf version , A4 size
        - white minimal professional -> have gimmick follow job/skill

    - sudoku game / chess theme
        - fill chess piece to fit condition
